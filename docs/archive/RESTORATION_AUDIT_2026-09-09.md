# Baseline restoration — 9 September 2026

> Historical review of an earlier checkpoint. For the current implementation, language coverage and validation, see the [documentation index](../README.md). Counts and release conclusions below apply only to the recorded checkpoint.

## Recovery and scope

Restoration base: `e7fdb43`, 5 September 2026, the latest commit before 8–9 September. Recent work was uncommitted. Before editing, all 295 changed/untracked paths were saved in a recovery archive outside this repository, together with a binary Git diff and the base revision.

The original 40-project catalogue, six guided shelves, full file explorer, three-stage build histories, case briefs, audit tabs, demo status and reading guidance have been restored. Following the user's refinement, GROWMAT opens its original showcase PDF and its added lab has been removed. The archive contains 16 guided experiences and 36 interactive project records. The discarded replacement list/story UI and conceptual additions remain recoverable in the recovery archives.

## Retained and integrated additions

- System 7 graph is the first archive view. The earlier dark 3D canvas, pastel topic clusters and perspective depth cues are restored. Drag rotates, Shift-drag pans, Fit view preserves the camera angle, and 2D remains optional. Raised buttons, inset panels, active blue states and system fonts match the site's System 7 controls. Focus, whole-graph, selection and fit changes use a 720 ms eased camera, 3D-position and opacity transition that preserves the viewing angle. Topics, methods, projects and dated work/education records can be traversed through the canvas, search and accessible relationship lists. The earlier Portfolio Map analysis remains available on demand inside this view.
- CV-backed experience/education includes separate Pfizer placement and product-owner roles, the degree records, research/teaching detail and independent technical work. Projects link back to the relevant timeline entries; career and document views link into projects and the graph. Related subject context is distinguished from direct role provenance.
- Finance retains the source-reviewed stateful import experiment and full fictional household ledger. Identity collisions, overlapping exports, repeated charges and reconciliation edge cases remain inspectable.
- RL restores the rich curriculum/evidence atlas while retaining seeded CliffWalking and recorded small-model post-training, LoRA and DPO exercises. Evidence is updated to the 6 September source; the recorded Week 25 failure remains disclosed.
- CFD restores architectures, tensor paths, model lineage and split-specific run evidence. Added controls play the FNO operator stages and the saved GNN vertical-velocity sequences. Horizontal velocity and pressure remain selectable.
- Microrobot architecture and benchmark views return alongside saved microscopy/Grad-CAM and the sequence-split experiment.
- MRI, scheduling, molecular matching, reliability and home-lab experiments remain additive to the complete original explanations. Source corrections include MRI relative-map ECE and the backup script’s 180-file retention count.
- GROWMAT's graph action and career links open the original showcase PDF. Its full project context remains accessible. Obsolete `view=demo` bookmarks resolve to the project file.
- CFD can pin a frame and compare later frames within the selected saved sequence, showing ordinal frame offsets and clearing the reference when the sequence changes. Microrobot can compare pose and depth attribution on one identical microscope input with a shared blend control, recorded task labels and source explanations of positive clipping and independent normalisation.
- MRI's uncertainty view now includes eight explicitly synthetic pixels: change uncertainty scale, reverse ranking and vary the number removed. Relative ECE follows notebook 03, cell 10 at revision `93bc9cd`; retained MSE is compared with an oracle using the same removal budget. The existing reported aggregate results remain separate and unchanged. All new MRI controls and explanations have Simplified and Traditional Chinese translations.
- Traditional Chinese graph copy uses 85 explicit translations, correcting terminology such as 關係圖 and 傅立葉. Search accepts the same Traditional Chinese terms shown in the interface; the 傅立葉 query was verified in the browser.
- Detailed Find once again indexes case briefs, build histories, status and evidence panels, with career context added.
- Actual mathematical expressions across the project studios now use shared KaTeX typesetting with accessible MathML and readable labels. Descriptions, numeric readouts, source evidence and genuine code/pseudocode remain intact. Longer equations use wider panels or aligned lines within the existing System 7 framing. The molecular greedy-assignment notation now follows the existing implementation: each predicted transition selects an unused observed transition; residual and RMS indices follow the accepted pairs. No matching computation changed.

## Evidence and performance boundaries

The animated fluid predictions are archived GNN outputs: 20 rollout frames and 30 simulation frames. FNO and U-Net archives contain still figures. The FNO animation explains the operator pipeline and does not claim newly generated trained-model forecasts. Field colours rescale per frame/channel and are not a common temporal magnitude scale.

The graph renders during interaction, resize and its finite 720 ms transitions; it has no idle animation loop. A new action interrupts from the currently displayed camera, positions and opacity. Reduced-motion preferences apply graph changes immediately. Specialist catalogue analysis and demo modules load on demand. Scientific playback initially loads only the chosen vertical-velocity channel (20 images rather than all 60 channel images for that sequence); other channels load when requested. Scientific animation starts manually and timers are cleaned up when their views unmount. Desktop architecture and deployment remain unchanged.

`MathEquation` shares KaTeX 0.18.7, with accessible MathML, 20 local WOFF2 fonts totalling 253.7 KiB and 30.6 KiB of local CSS. There is no CDN dependency. Its 257.1 KiB shared parser/renderer chunk loads on demand; the build gate enforces a separate 300 KiB cap and verifies that no initial page entry includes it. The existing 4 MiB non-math browser budget and 4 MiB application-runtime budget remain in place.

## Verification

- `npm run lint` and `npm run build:isolated` pass; the build generates 83 static pages and preserves dynamic exact-project routes.
- Production output: 111 JavaScript/CSS/browser files, 4.05 MiB in total including the separately capped 257.1 KiB math-renderer chunk; traced application runtime 3.72 MiB. Next reports an unchanged 249 kB first-load JavaScript for the desktop/project routes. These are build-size measurements, not claims of measured field loading speed or Core Web Vitals.
- Detailed Find indexes: 518.1–693.9 KiB per locale, fetched only when requested. All 17 search checks pass, including retained narratives, translated aliases and isolation.
- All existing artifact, fixture, desktop, select, orbital and locale gates pass. Catalogue completeness reconciles 40 files, 36 interactive records, 16 experiences and six shelves. Locale coverage includes 667 core System 7 strings.
- Graph: 27 checks, 82 nodes and 188 edges; deterministic positioning, typed relationships, date retention, accessible hit targets, bounded large-catalogue rendering, exact destination URLs, 3D rotation/fitting and finite transition behaviour.
- Math checks pass for static and generated expressions, strict parsing, accessible MathML, escaped fallback output and the reviewed local font set. The output gate confirms a single shared renderer chunk within its cap and excludes it from initial page entries.
- Scientific media: 11 checks over 180 exact-hash images; 150 images form the complete multichannel flow sequences. FNO walkthrough, channel-selective preload, same-input attribution and pinned-frame controls are covered.
- Learning: 148 source transition fixtures plus seeded learning/LoRA/DPO/coverage/recorded-answer checks. Eight source-experiment suites, seven original-finance fixture attempts and 70 MRI uncertainty configurations pass; MRI controls are checked against independent arithmetic.
- Browser review on the production build verified FNO phase progression (Modes → rFFT observed while playing), GNN vertical-velocity frame progression, pause controls, exact Home Lab demo launch instead of the desktop overview, reload and browser Back to the selected graph node. Finance import inserted two rows; replay added zero and retained £75.00.
- Desktop graph traversal, timeline-to-degree navigation, Chinese graph deep links and 390px rendering were checked. Chinese education and its graph destination both have document width equal to the 390px viewport. No browser console errors were captured during the final review.
- Two compiled CV pages were visually reviewed against the source. Eleven dated career/education anchors and 34 project links reconcile; related-context links remain explicit.
- Follow-up browser review verified 3D rotation and 2D switching, the GROWMAT graph PDF action and obsolete demo URL fallback. CFD frame 1 remained pinned while frame 2 showed offset +1; switching to simulation cleared the reference. Production microscopy comparison showed the same sample in both task panels and responded to the shared blend control. MRI at 8× uncertainty retained ECE 0.0000; reversed ranking changed retained MSE from 0.005925 to 0.113725 while the equal-budget oracle remained 0.005925. The 390px Chinese MRI and graph views have document width equal to their viewport. No browser console errors were captured.
- The latest animation/typesetting browser review verified intermediate and settled 3D focus/whole-graph transitions on the production build, including rapid reversal. Desktop Stock and PC-SAFT equations and embedded MeshGraphNet diagram formulas fit their panels. Live LoRA changed loss from 1 to 0.81 after one step; matching the DPO reference restored loss 0.693147. LoRA, DPO and PC-SAFT formulas fit at 390px without local equation or document overflow. The compact project header now gives its title a full row above the two navigation buttons. Production PC-SAFT rendered all 18 expressions at 390px with no math errors; the production graph/math review captured no browser console errors. These are targeted follow-up checks; the broader restoration reviews above describe their own tested views.

The work is local and reviewable. No production deployment or Git history rewrite was performed. Recovery snapshots are at `/Users/samuel/GitHub/samuel-homepage-recovery/20260909-022615` and `/Users/samuel/GitHub/samuel-homepage-recovery/20260909-025841-before-3d-refinement`.
